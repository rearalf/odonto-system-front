import type { LucideIcon } from 'lucide-react';

export type FieldMockProps = {
  label: string;
  placeholder: string;
  badge?: string;
  leftIcon: LucideIcon;
  help?: string;
  error?: string;
};
